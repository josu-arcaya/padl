version: "1.0"
pipeline:
  regressionLineal:
    model: regression.pfa
    constraints:
      node:
        layer: edge
        os: windows
        hostname: kafka01.example.com
      model:
        max_execution_time: 600s
